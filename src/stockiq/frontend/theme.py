# Single source of truth for the dark-theme colour palette used in every
# st.markdown() / st.html() HTML string across the frontend.
UP  = "#22C55E"   # green  — bullish / positive
DN  = "#EF4444"   # red    — bearish / negative
NEU = "#F59E0B"   # amber  — neutral / caution
MUT = "#64748B"   # slate  — muted label text
VAL = "#F1F5F9"   # near-white — primary value text
BG  = "#0F172A"   # deep navy — card background
SEP = "#1E293B"   # dark slate — border / separator
