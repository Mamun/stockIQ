"""
views/components — reusable Streamlit UI components shared across pages.

  components.charts       — Plotly chart builders (candlestick + indicators)
  components.gap_table    — Gap fill tracker table (SPY, stock, VIX)
  components.summary_card — Price + technicals summary card
"""
